import java.io.*;
import java.util.Arrays;
public class file {

   public static void main(String args[])throws IOException {
      
      Boolean[] arr = new Boolean[book];
      Arrays.fill(arr, Boolean.FALSE);
      for(i=0;i<arr1.length();i++){
         int no=arr1[i];
         if(arr[no]==false){
            arr[no]=true;
         }
      }

   }
}